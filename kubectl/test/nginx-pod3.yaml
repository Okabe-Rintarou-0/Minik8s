apiVersion: v1
kind: Pod
metadata:
  name: not-nginx
  namespace: default
  labels:
    name: not-nginx
spec:
  containers:
    - name: nginx
      image: nginx:latest
      ports:
        - containerPort: 80